class Gops:
    def __init__(self):

    def genericImports():
        import sys
        import math
        import hashlib
        import itertools

    def createDictWithLists(l1[],l2[]):
        for i,j in zip(l1,l2):
            dictionnary += {i:j}
        return dictionnary

        
